Adding a .editorconfig file is a great repository health practice. It helps formatters and IDEs automatically enforce indent types (spaces vs tabs), size, and line endings consistently across different contributors' environments.

